machine:
    environment:
        GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx2048m -XX:MaxPermSize=1024m -Xms512m -XX:+HeapDumpOnOutOfMemoryError"'
#        ANDROID_HOME: /home/ubuntu/android

dependencies:
    cache_directories:
#        - ~/.android
#        - ~/android
#        - /usr/local/android-sdk-linux
    pre:
        - ./install-dependencies.sh
#        - echo y | android update sdk --no-ui --all --filter "build-tools-23.0.1,extra-android-support,extra-google-google_play_services,extra-google-m2repository,extra-android-m2repository"
#        - echo y | android update sdk --no-ui --all --filter tool && echo y | android update sdk --no-ui --all --filter "tools,platform-tools,build-tools-23.0.3,extra-android-support,extra-google-google_play_services,extra-google-m2repository,extra-android-m2repository"

test:
    override:
        - ./gradlew clean assembleDebug
